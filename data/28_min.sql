--                                         MIN()  Functions
-----------------------------------------------------------------------------------------------------------------------
-- The SQL MIN() Functions ;
-- The MIN() function returns the smallest value of the selected column.
-----------------------------------------------------------------------------------------------------------------------
-- Syntax
-----------------------------------------------------------------------------------------------------------------------
-- SELECT MIN(column_name)
-- FROM table_name
-- WHERE condition;
-----------------------------------------------------------------------------------------------------------------------
create database wwwww;
use wwwww;
create table emp(id int,
name varchar(30),salary float );

-----------------------------------------------------------------------------------------------------------------------
insert into emp value(1,'roshani',40000);
insert into emp value(2,'rani',450000);
insert into emp value(3,'sakshi',940000);
insert into emp value(4,'divya',78000);

select min(salary) from emp;
-----------------------------------------------------------------------------------------------------------------------
-- use alias
select min(salary) as minsalary  from emp;

-----------------------------------------------------------------------------------------------------------------------
-- use min group by  
select min(salary) as minsalary , id from emp group by id ;

-----------------------------------------------------------------------------------------------------------------------
